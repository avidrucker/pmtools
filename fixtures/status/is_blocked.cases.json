[
  { "name": "the `blocked` label present → true",
    "args": [["bug", "blocked"]],
    "expected": true },
  { "name": "`blocked` is the only label → true",
    "args": [["blocked"]],
    "expected": true },
  { "name": "no `blocked` label → false",
    "args": [["bug", "enhancement", "area:status"]],
    "expected": false },
  { "name": "empty label list → false",
    "args": [[]],
    "expected": false },
  { "name": "null labels (degrade-safe) → false",
    "args": [null],
    "expected": false },
  { "name": "exact-match: capitalised `Blocked` is not the canonical label → false",
    "args": [["Blocked"]],
    "expected": false },
  { "name": "blocked-by relation (count>0) with no `blocked` label → true (#87)",
    "args": [["bug"], 1],
    "expected": true },
  { "name": "blocked-by count of several → true (#87)",
    "args": [[], 3],
    "expected": true },
  { "name": "no label and zero blocked-by count → false (#87)",
    "args": [["bug"], 0],
    "expected": false },
  { "name": "`blocked` label present even with zero blocked-by count → true (#87)",
    "args": [["blocked"], 0],
    "expected": true },
  { "name": "null labels but a blocked-by relation → true (degrade-safe, #87)",
    "args": [null, 2],
    "expected": true }
]
