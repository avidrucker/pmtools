[
  { "name": "origin scenario: pmtools claim + npm run close → fire the note",
    "args": ["pmtools claim", "npm run close"],
    "expected": { "warn": "claiming with pmtools (br-/wt- branch names) but closeCommand is \"npm run close\"; ensure it accepts br-/wt- branch names or the close will be rejected." } },
  { "name": "both pmtools → coherent, no note",
    "args": ["pmtools claim", "pmtools close"],
    "expected": null },
  { "name": "full-path pmtools close still matches (substring) → no note",
    "args": ["pmtools claim", "/usr/local/bin/pmtools close"],
    "expected": null },
  { "name": "full-path pmtools claim + non-pmtools close → fire",
    "args": ["/usr/local/bin/pmtools claim", "make close"],
    "expected": { "warn": "claiming with pmtools (br-/wt- branch names) but closeCommand is \"make close\"; ensure it accepts br-/wt- branch names or the close will be rejected." } },
  { "name": "claimCommand not pmtools → no note (consumer isn't on pmtools claim)",
    "args": ["npm run claim", "npm run close"],
    "expected": null },
  { "name": "both null (pmtools-own / unconfigured) → no note",
    "args": [null, null],
    "expected": null },
  { "name": "pmtools claim + null close → no note (no conflicting close configured)",
    "args": ["pmtools claim", null],
    "expected": null },
  { "name": "pmtools claim + empty-string close → no note",
    "args": ["pmtools claim", ""],
    "expected": null },
  { "name": "case-insensitive: PMTOOLS Claim + npm run close → fire",
    "args": ["PMTOOLS Claim", "npm run close"],
    "expected": { "warn": "claiming with pmtools (br-/wt- branch names) but closeCommand is \"npm run close\"; ensure it accepts br-/wt- branch names or the close will be rejected." } },
  { "name": "non-string claim (number) → no note (tolerant)",
    "args": [42, "npm run close"],
    "expected": null }
]
