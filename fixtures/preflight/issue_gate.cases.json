[
  { "name": "OPEN → proceed (no warn, no error)",
    "args": ["OPEN"], "expected": { "ok": true } },
  { "name": "is case/whitespace tolerant (  open  )",
    "args": ["  open  "], "expected": { "ok": true } },
  { "name": "CLOSED → block with a loud error",
    "args": ["CLOSED"],
    "expected": { "ok": false, "error": "issue is CLOSED, not OPEN — nothing to start (raced a close?). Pick another issue." } },
  { "name": "MERGED → block",
    "args": ["MERGED"],
    "expected": { "ok": false, "error": "issue is MERGED, not OPEN — nothing to start (raced a close?). Pick another issue." } },
  { "name": "LOCKED → block",
    "args": ["LOCKED"],
    "expected": { "ok": false, "error": "issue is LOCKED, not OPEN — nothing to start (raced a close?). Pick another issue." } },
  { "name": "null state → proceed with a warning (offline best-effort)",
    "args": [null],
    "expected": { "ok": true, "warn": "issue state unknown (gh unavailable) — proceeding best-effort." } },
  { "name": "empty string → proceed with a warning",
    "args": [""],
    "expected": { "ok": true, "warn": "issue state unknown (gh unavailable) — proceeding best-effort." } },
  { "name": "whitespace-only → proceed with a warning",
    "args": ["   "],
    "expected": { "ok": true, "warn": "issue state unknown (gh unavailable) — proceeding best-effort." } }
]
